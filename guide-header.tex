% ── Page setup ──
\usepackage[margin=2.2cm]{geometry}
\usepackage{fancyhdr}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{tcolorbox}
\usepackage{fontspec}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{float}

% ── Colors ──
\definecolor{primary}{HTML}{58A6FF}
\definecolor{surface}{HTML}{161B22}
\definecolor{bg}{HTML}{0D1117}
\definecolor{accent}{HTML}{3FB950}
\definecolor{warn}{HTML}{D29922}
\definecolor{alert}{HTML}{F85149}
\definecolor{muted}{HTML}{8B949E}
\definecolor{codebg}{HTML}{1A1F2B}
\definecolor{linkblue}{HTML}{58A6FF}

% ── Hyperlinks ──
\hypersetup{
  colorlinks=true,
  linkcolor=linkblue,
  urlcolor=linkblue,
  citecolor=linkblue
}

% ── Fonts ──
\setmainfont{DejaVu Sans}
\setmonofont{DejaVu Sans Mono}[Scale=0.85]

% ── Headers/footers ──
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\color{muted}Guide IAQ V2 — Surveillance qualité de l'air}
\fancyhead[R]{\small\color{muted}\thepage}
\fancyfoot[C]{\small\color{muted}Version 2 — Mars 2026 — MH-Z19 / ADS1115 / Render}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.2pt}

% ── Section styling ──
\titleformat{\section}
  {\Large\bfseries\color{primary}}
  {\thesection}{0.8em}{}[\vspace{-0.3em}\textcolor{primary}{\rule{\textwidth}{0.6pt}}]

\titleformat{\subsection}
  {\large\bfseries\color{accent}}
  {\thesubsection}{0.6em}{}

\titleformat{\subsubsection}
  {\normalsize\bfseries\color{warn}}
  {\thesubsubsection}{0.5em}{}

% ── Tighter lists ──
\setlist{itemsep=2pt, parsep=1pt}

% ── Table styling ──
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}

% ── Custom title page ──
\renewcommand{\maketitle}{
  \begin{titlepage}
    \centering
    \vspace*{3cm}

    {\fontsize{14}{16}\selectfont\color{muted}GUIDE COMPLET}

    \vspace{0.8cm}

    {\fontsize{32}{38}\selectfont\bfseries\color{primary}Tableau de bord IAQ}

    \vspace{0.3cm}

    {\fontsize{18}{22}\selectfont\bfseries\color{accent}VERSION 2}

    \vspace{0.4cm}

    {\fontsize{16}{20}\selectfont\color{white}Surveillance de la qualité\\ de l'air intérieur}

    \vspace{1.5cm}

    \begin{tikzpicture}
      \draw[primary, line width=2pt] (-3,0) -- (3,0);
    \end{tikzpicture}

    \vspace{1.5cm}

    {\large\color{accent}ESP32 + Flask + Chart.js + Render Cloud}

    \vspace{0.6cm}

    {\color{muted}MH-Z19 \quad CCS811 \quad MQ-7 (ADS1115) \quad DHT22}

    \vspace{0.3cm}

    {\color{muted}CO\textsubscript{2} \quad TVOC \quad CO \quad Température \quad Humidité}

    \vspace{3cm}

    {\color{muted}\small Version 2 — Mars 2026}

    \vspace{0.3cm}

    {\color{muted}\small HTTPS · OTA · LittleFS · Email Gmail · Render}

  \end{titlepage}
}

% ── Code block wrapping (prevent overflow) ──
\usepackage{fvextra}
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{breaklines,breakanywhere,commandchars=\\\{\},fontsize=\small}
\tcbuselibrary{listings, breakable}
